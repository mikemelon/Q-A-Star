# 数据库MySQL 5.5.61 是本地的MySQL2服务
spring:
   datasource:
      url: jdbc:mysql://localhost:3306/qadb2
      username: root
      password: root
      driver-class-name: com.mysql.jdbc.Driver
   jpa:
      hibernate:
         ddl-auto: update
         dialect: org.hibernate.dialect.MySQL5Dialect
      show-sql: true
      database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
      
logging:
   level:
      cn:
         lynu:
            lyq:
               qastar: DEBUG